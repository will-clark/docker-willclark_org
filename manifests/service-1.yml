apiVersion: v1
kind: Service
metadata:
    name: "aks-f7fb"
    labels:
        app: "aks-f7fb"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aks-f7fb"