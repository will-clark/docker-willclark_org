apiVersion: v1
kind: Service
metadata:
    name: "aks-065e"
    labels:
        app: "aks-065e"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aks-065e"